import React,{useEffect,useMemo,useState}from'react';import{LayoutDashboard,FilePlus2,ClipboardList,LogOut,CheckCircle2,Clock3,XCircle,RotateCcw,Users,Wifi,Search}from'lucide-react';
const API='http://localhost:5000/api';
const stages=['Pending SDU & Network','Pending Head of Commercial','Pending Finance','Completed'];
const requestTypes=['Upgrade','Downgrade','Seasonal Upgrade','Seasonal Downgrade','Disconnection','Reconnection'];
async function call(path,opts={}){const r=await fetch(API+path,{headers:{'Content-Type':'application/json',...(opts.headers||{})},...opts});const d=await r.json();if(!r.ok)throw new Error(d.error||'Request failed');return d}
function Login({onLogin}){const[email,setEmail]=useState('kam@zanlink.co.tz'),[password,setPassword]=useState('demo123'),[err,setErr]=useState('');async function submit(e){e.preventDefault();try{onLogin(await call('/login',{method:'POST',body:JSON.stringify({email,password})}))}catch(x){setErr(x.message)}}return <div className="login"><div className="loginCard"><div className="brandMark">Z</div><h1>ZanLink Project 2</h1><p>Customer Account Change & Approval System</p><form onSubmit={submit}><label>Email<input value={email}onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password}onChange={e=>setPassword(e.target.value)}/></label>{err&&<div className="error">{err}</div>}<button>Sign in</button></form><div className="demo"><b>Demo:</b> kam@zanlink.co.tz / demo123</div></div></div>}
function Sidebar({view,setView,user,onLogout}){return <aside><div className="sideBrand"><div className="brandMark small">Z</div><div><b>ZanLink</b><span>Project 2</span></div></div><nav>{[['dashboard',LayoutDashboard,'Dashboard'],['new',FilePlus2,'New Request'],['requests',ClipboardList,'Requests']].map(([v,I,t])=><button className={view===v?'active':''}onClick={()=>setView(v)} key={v}><I size={19}/>{t}</button>)}</nav><div className="profile"><div className="avatar">{user.name[0]}</div><div><b>{user.name}</b><span>{user.role}</span></div><button title="Logout"onClick={onLogout}><LogOut size={18}/></button></div></aside>}
function Status({s}){let I=s==='Completed'?CheckCircle2:s==='Rejected'?XCircle:s==='Returned'?RotateCcw:Clock3;return <span className={'status '+s.toLowerCase().replaceAll(' ','-').replaceAll('&','')}><I size={14}/>{s}</span>}
function Dashboard({rows,user,onOpen,setView}){const count=s=>rows.filter(r=>r.status===s).length;return <><header><div><h1>Dashboard</h1><p>Customer account requests and approval workflow</p></div><button className="primary"onClick={()=>setView('new')}>+ New Request</button></header><div className="cards"><Card t="Total Requests"n={rows.length} I={ClipboardList}/><Card t="SDU & Network"n={count('Pending SDU & Network')} I={Wifi}/><Card t="Commercial"n={count('Pending Head of Commercial')} I={Users}/><Card t="Finance"n={count('Pending Finance')} I={Clock3}/><Card t="Completed"n={count('Completed')} I={CheckCircle2}/></div><section className="panel"><div className="panelHead"><div><h2>Recent Requests</h2><p>Track requests from submission to completion.</p></div></div><RequestTable rows={rows.slice(0,8)} onOpen={onOpen}/></section></>}
function Card({t,n,I}){return <div className="card"><div className="icon"><I size={21}/></div><div><span>{t}</span><strong>{n}</strong></div></div>}
function RequestTable({rows,onOpen}){return <div className="tableWrap"><table><thead><tr><th>Request</th><th>Customer</th><th>Type</th><th>Change</th><th>Status</th><th>Created</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}onClick={()=>onOpen(r)}><td><b>{r.ref}</b></td><td>{r.customer_name}<small>{r.customer_id}</small></td><td>{r.request_type}</td><td>{r.request_type.includes('connection')?r.request_type:<>{r.current_capacity||'-'} → {r.new_capacity||'-'}</>}</td><td><Status s={r.status}/></td><td>{r.created_at?.slice(0,10)}</td></tr>)}{!rows.length&&<tr><td colSpan="6"className="empty">No requests found.</td></tr>}</tbody></table></div>}
function NewRequest({user,onCreated}){const[f,setF]=useState({request_type:'Upgrade',customer_name:'',customer_id:'',month:'',ip_radius:'IP',radius_username:'',client_segment:'Enterprise',revenue_type:'DIA',mode_of_service:'Fiber',effective_date:'',current_capacity:'',new_capacity:'',current_price:'',new_price:'',reason:'',comments:''}),[msg,setMsg]=useState('');const service=['Disconnection','Reconnection'].includes(f.request_type);function upd(k,v){setF({...f,[k]:v})}async function submit(e){e.preventDefault();try{await call('/requests',{method:'POST',headers:{'X-User-Id':user.id},body:JSON.stringify(f)});setMsg('Request submitted successfully.');setTimeout(onCreated,700)}catch(x){setMsg(x.message)}}return <><header><div><h1>New Customer Account Request</h1><p>Commercial / KAM initiates the request. Approval routing is automatic.</p></div></header><form className="form panel"onSubmit={submit}><div className="sectionTitle"><b>1. Request Type</b><span>Select the customer account action required.</span></div><div className="typeGrid">{requestTypes.map(t=><button type="button"key={t}className={f.request_type===t?'selected':''}onClick={()=>upd('request_type',t)}>{t}</button>)}</div><div className="sectionTitle"><b>2. Customer Details</b><span>Required account and service information.</span></div><div className="grid"><Field l="Customer Name *"v={f.customer_name}set={v=>upd('customer_name',v)}/><Field l="Customer ID *"v={f.customer_id}set={v=>upd('customer_id',v)}/><Field l="Month"v={f.month}set={v=>upd('month',v)}type="month"/><Select l="IP / Radius"v={f.ip_radius}set={v=>upd('ip_radius',v)}opts={['IP','Radius','Local Loop','IP and Radius']}/><Field l="Radius Username"v={f.radius_username}set={v=>upd('radius_username',v)}/><Select l="Client Segment"v={f.client_segment}set={v=>upd('client_segment',v)}opts={['Enterprise','Retail','Wholesale','Intercompany']}/><Select l="Revenue Type"v={f.revenue_type}set={v=>upd('revenue_type',v)}opts={['DIA','Local Loop','Seasonal']}/><Select l="Mode of Service"v={f.mode_of_service}set={v=>upd('mode_of_service',v)}opts={['Fiber','Wireless']}/><Field l="Effective / Billing Date *"v={f.effective_date}set={v=>upd('effective_date',v)}type="date"/></div>{!service&&<><div className="sectionTitle"><b>3. Capacity & Price Change</b><span>Record current and requested commercial terms.</span></div><div className="grid"><Field l="Current Capacity / Plan *"v={f.current_capacity}set={v=>upd('current_capacity',v)}ph="e.g. 20 Mbps"/><Field l="New Capacity / Plan *"v={f.new_capacity}set={v=>upd('new_capacity',v)}ph="e.g. 50 Mbps"/><Field l="Current Price *"v={f.current_price}set={v=>upd('current_price',v)}type="number"/><Field l="New Price *"v={f.new_price}set={v=>upd('new_price',v)}type="number"/></div></>}<div className="sectionTitle"><b>{service?'3':'4'}. Request Notes</b><span>Provide the reason and any additional information.</span></div><div className="grid"><Field l="Reason *"v={f.reason}set={v=>upd('reason',v)}/><Field l="Comments"v={f.comments}set={v=>upd('comments',v)}/></div><div className="flow"><b>Approval route</b><span>KAM</span>→<span>Head of SDU & Network</span>→<span>Head of Commercial</span>→<span>Finance</span>→<span>Completed</span></div>{msg&&<div className="notice">{msg}</div>}<div className="actions"><button className="primary">Submit Request</button></div></form></>}
function Field({l,v,set,type='text',ph=''}){return <label>{l}<input required={l.includes('*')}type={type}placeholder={ph}value={v}onChange={e=>set(e.target.value)}/></label>};function Select({l,v,set,opts}){return <label>{l}<select value={v}onChange={e=>set(e.target.value)}>{opts.map(x=><option key={x}>{x}</option>)}</select></label>}
function Requests({rows,onOpen}){const[q,setQ]=useState('');const filtered=rows.filter(r=>(r.customer_name+r.ref+r.request_type+r.status).toLowerCase().includes(q.toLowerCase()));return <><header><div><h1>Requests</h1><p>Search and follow all customer account changes.</p></div></header><section className="panel"><div className="search"><Search size={18}/><input placeholder="Search customer, request ID, type or status..."value={q}onChange={e=>setQ(e.target.value)}/></div><RequestTable rows={filtered}onOpen={onOpen}/></section></>}
function Detail({r,user,onBack,onChanged}){const[comment,setComment]=useState(''),[msg,setMsg]=useState('');const canApprove=(user.role==='Head of SDU & Network'&&r.status==='Pending SDU & Network')||(user.role==='Head of Commercial'&&r.status==='Pending Head of Commercial')||(user.role==='Finance'&&r.status==='Pending Finance');async function act(action){try{await call(`/requests/${r.id}/action`,{method:'POST',headers:{'X-User-Id':user.id},body:JSON.stringify({action,comment})});onChanged()}catch(x){setMsg(x.message)}}return <><header><div><button className="back"onClick={onBack}>← Back to requests</button><h1>{r.ref}</h1><p>{r.customer_name} · {r.request_type}</p></div><Status s={r.status}/></header><div className="detailGrid"><section className="panel"><h2>Request Details</h2><div className="facts">{[['Customer',r.customer_name],['Customer ID',r.customer_id],['Request Type',r.request_type],['Effective Date',r.effective_date],['IP / Radius',r.ip_radius],['Segment',r.client_segment],['Revenue Type',r.revenue_type],['Mode of Service',r.mode_of_service],['Current Capacity / Plan',r.current_capacity||'—'],['New Capacity / Plan',r.new_capacity||'—'],['Current Price',r.current_price||'—'],['New Price',r.new_price||'—'],['Reason',r.reason],['Comments',r.comments||'—'],['Submitted by',r.created_by_name]].map(([a,b])=><div key={a}><span>{a}</span><b>{b}</b></div>)}</div></section><section className="panel"><h2>Approval Progress</h2><div className="timeline">{r.history?.map((h,i)=><div key={i}className="timelineItem"><div className="dot"></div><div><b>{h.action}</b><span>{h.actor_name} · {h.actor_role}</span><small>{h.created_at}{h.comment?' · '+h.comment:''}</small></div></div>)}</div>{canApprove&&<div className="approval"><h3>Action Required</h3><textarea placeholder="Approval / return comment"value={comment}onChange={e=>setComment(e.target.value)}/><div><button className="approve"onClick={()=>act('approve')}>{user.role==='Finance'?'Mark Implemented':'Approve'}</button><button className="return"onClick={()=>act('return')}>Return</button><button className="reject"onClick={()=>act('reject')}>Reject</button></div></div>}{msg&&<div className="error">{msg}</div>}</section></div></>}
export default function App(){const[user,setUser]=useState(()=>JSON.parse(localStorage.getItem('zp2-user')||'null')),[view,setView]=useState('dashboard'),[rows,setRows]=useState([]),[selected,setSelected]=useState(null);async function refresh(){if(!user)return;setRows(await call('/requests',{headers:{'X-User-Id':user.id}}))}useEffect(()=>{refresh()},[user]);if(!user)return <Login onLogin={u=>{localStorage.setItem('zp2-user',JSON.stringify(u));setUser(u)}}/>;function logout(){localStorage.removeItem('zp2-user');setUser(null)}function open(r){setSelected(r);setView('detail')}async function changed(){await refresh();const all=await call('/requests',{headers:{'X-User-Id':user.id}});const n=all.find(x=>x.id===selected.id);setRows(all);setSelected(n)}return <div className="app"><Sidebar view={view}setView={v=>{setView(v);setSelected(null)}}user={user}onLogout={logout}/><main>{view==='dashboard'&&<Dashboard rows={rows}user={user}onOpen={open}setView={setView}/>} {view==='new'&&<NewRequest user={user}onCreated={()=>{refresh();setView('requests')}}/>}{view==='requests'&&<Requests rows={rows}onOpen={open}/>} {view==='detail'&&selected&&<Detail r={selected}user={user}onBack={()=>setView('requests')}onChanged={changed}/>}</main></div>}
